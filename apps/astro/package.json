{
  "name": "@dw/astro",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "astro build",
    "build:local": "pnpm with-env astro build",
    "dev": "astro dev",
    "preview": "astro preview",
    "typecheck": "astro check",
    "lint": "eslint --flag unstable_native_nodejs_ts_config",
    "astro:unit:test": "vitest run --config vitest.unit.config.ts",
    "with-env": "dotenv -e ../../.env.local --",
    "clean": "git clean -xdf dist .astro node_modules"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "@astrojs/vercel": "^10.0.8",
    "astro": "^7.0.3"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "@dw/eslint-config": "workspace:*",
    "eslint": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
