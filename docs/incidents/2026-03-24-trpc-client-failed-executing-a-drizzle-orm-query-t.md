# Incident: tRPC client failed executing a Drizzle ORM query to select posts from the database, likely due to a database connection or query execution error

| Field | Value |
|---|---|
| **Date** | 2026-03-24 |
| **Type** | Runtime Error |
| **Severity** | 🟠 HIGH |
| **Service** | `dw-portfolio-production` |
| **GitHub Issue** | https://imperial-coal.sentry.io/issues/7360781158/ |

## Summary
tRPC client failed executing a Drizzle ORM query to select posts from the database, likely due to a database connection or query execution error

## Root Cause
The error shows a failed SQL query "select id, title, content, created_at, author_id, updated_at from post Post order by Post.id desc limit $1" which suggests either a database connection failure, incorrect table schema, or missing database permissions. Without a stacktrace, this is likely happening in a tRPC procedure that fetches posts, probably in a posts.getAll or similar endpoint.

## Impact
1 user affected, posts listing/fetching feature is broken preventing users from viewing blog posts or portfolio content

## Resolution
1. Check Supabase database connection and ensure the "post" table exists with correct schema 2. Verify database credentials and connection string in environment variables 3. Add proper error handling and logging to the tRPC posts query procedure 4. Check if the Drizzle schema matches the actual database table structure 5. Test the raw SQL query directly in Supabase to isolate if it's a query or connection issue

## Context
- **Error**: `TRPCClientError: Failed query: select "id", "title", "content", "created_at", "author_id", "updated_at" from "post" "Post" order by "Post"."id" desc limit $1`
- **Culprit**: `/`
- **Environment**: `unknown`

- **Users affected**: 1
- **First seen**: 2026-03-24T17:26:11.977000+00:00
- **Sentry**: https://imperial-coal.sentry.io/issues/7360781158/

---
*Generated by platform-agent on 2026-03-24T17:26:59.760Z*
*Labels: database, trpc, drizzle, supabase, query-failure*
