# Incident: TRPCError occurred during a posts query with ORDER BY and LIMIT clause, likely from a database connection or permission issue on OPTIONS preflight request

| Field | Value |
|---|---|
| **Date** | 2026-04-14 |
| **Type** | Runtime Error |
| **Severity** | 🟠 HIGH |
| **Service** | `dw-portfolio-preview` |
| **GitHub Issue** | https://imperial-coal.sentry.io/issues/7412304345/ |

## Summary
TRPCError occurred during a posts query with ORDER BY and LIMIT clause, likely from a database connection or permission issue on OPTIONS preflight request

## Root Cause
The error shows a failed SELECT query on the "post" table with ordering and pagination. Since it's associated with "OPTIONS /" and no stacktrace is available, this appears to be a CORS preflight request triggering a tRPC procedure that's failing at the database level. The query syntax suggests Drizzle ORM with PostgreSQL, but the failure could be due to Supabase connection issues, missing RLS policies, or the database user lacking SELECT permissions on the post table.

## Impact
1 user affected, posts listing feature is broken - users cannot view the blog/portfolio posts

## Resolution
1. Check Supabase RLS policies on the "post" table to ensure SELECT is allowed for the current auth context. 2. Verify database connection string and credentials in environment variables. 3. Add proper error handling in the posts tRPC router to catch and log database failures. 4. Ensure CORS is properly configured for the domain making OPTIONS requests. 5. Check if the "post" table exists and has the expected columns (id, title, content, created_at, author_id, updated_at).

## Context
- **Error**: `TRPCError: Failed query: select "id", "title", "content", "created_at", "author_id", "updated_at" from "post" "Post" order by "Post"."id" desc limit $1`
- **Culprit**: `OPTIONS /`
- **Environment**: `unknown`

- **Users affected**: 1
- **First seen**: 2026-04-14T22:28:08.972000+00:00
- **Sentry**: https://imperial-coal.sentry.io/issues/7412304345/

---
*Generated by platform-agent on 2026-04-14T22:38:03.350Z*
*Labels: database, trpc, supabase, cors, posts*
