# Incident: TRPCClientError occurred on GET /admin route due to server returning HTML instead of JSON, likely from a 404/500 error page or auth redirect

| Field | Value |
|---|---|
| **Date** | 2026-06-02 |
| **Type** | Runtime Error |
| **Severity** | 🟠 HIGH |
| **Service** | `dw-portfolio-preview` |
| **GitHub Issue** | https://imperial-coal.sentry.io/issues/7521013452/ |

## Summary
TRPCClientError occurred on GET /admin route due to server returning HTML instead of JSON, likely from a 404/500 error page or auth redirect

## Root Cause
The tRPC client expected JSON response but received HTML (starting with "<!doctype"). This typically happens when: 1) The tRPC API endpoint is not properly configured or missing, 2) Server returns an error page instead of JSON, 3) Auth middleware redirects to login page, or 4) Next.js returns a 404 page. The /admin route likely has a tRPC query that's hitting a non-existent or misconfigured API endpoint.

## Impact
1 user affected, admin dashboard completely broken - user cannot access administrative features

## Resolution
1. Check if tRPC router is properly exported in app/api/trpc/[trpc]/route.ts, 2. Verify admin page tRPC queries point to existing procedures, 3. Add error boundaries around tRPC queries in admin components, 4. Check Clerk auth middleware isn't redirecting API calls, 5. Add proper error handling: ```tsx
// In admin page component
const { data, error } = api.admin.getData.useQuery(undefined, {
  retry: false,
  onError: (err) => console.error('Admin query failed:', err)
});
if (error) return <div>Failed to load admin data</div>;
```

## Context
- **Error**: `TRPCClientError: Unexpected token '<', "<!doctype "... is not valid JSON`
- **Culprit**: `GET /admin`
- **Environment**: `unknown`

- **Users affected**: 1
- **First seen**: 2026-06-02T10:56:23.030000+00:00
- **Sentry**: https://imperial-coal.sentry.io/issues/7521013452/

---
*Generated by platform-agent on 2026-06-02T10:57:04.574Z*
*Labels: trpc, admin, json-parsing, api-error, auth*
