# Incident: JSON parsing failed on GET /admin route because the response returned HTML (likely an error page) instead of expected JSON data

| Field | Value |
|---|---|
| **Date** | 2026-06-04 |
| **Type** | Runtime Error |
| **Severity** | 🟠 HIGH |
| **Service** | `unknown` |
| **GitHub Issue** |  |

## Summary
JSON parsing failed on GET /admin route because the response returned HTML (likely an error page) instead of expected JSON data

## Root Cause
The /admin route is attempting to parse a response as JSON using JSON.parse() or fetch().json(), but the server returned an HTML document (starting with "<!doctype") instead of JSON. This typically occurs when an API endpoint returns an error page, redirect, or when fetching from a non-API route that serves HTML.

## Impact
Admin panel is completely broken for any users trying to access it, though currently 0 users affected in preview environment

## Resolution
1. Check if /admin route is correctly configured in app/admin/page.tsx or pages/admin/index.tsx 2. Verify any fetch calls in the admin component have proper error handling and are calling the correct API endpoints (should be /api/* routes) 3. Add try-catch around JSON.parse() calls and check response.ok before calling .json() 4. If using tRPC, ensure the admin route is using tRPC client correctly rather than raw fetch calls

## Context
- **Error**: `Unknown error`
- **Culprit**: `GET /admin`
- **Environment**: `vercel-preview`
- **Route**: `https://dev.dw-portfolio.dev/admin`
- **Users affected**: 0
- **First seen**: 2026-06-04T04:10:36.154Z
- **Sentry**: 

---
*Generated by platform-agent on 2026-06-04T04:10:45.434Z*
*Labels: json-parsing, admin-panel, api-error, fetch-error*
