# Incident: Recurrence: CI job logs for the "CI" workflow on branch dev could not be retrieved (HTTP 404), identical to the incident on 2026-07-12 at commit 9bfedf1, so the actual failure at commit 06eefc2 cannot be determined.

| Field | Value |
|---|---|
| **Date** | 2026-07-14 |
| **Type** | CI Failure |
| **Severity** | 🟠 HIGH |
| **Service** | `dw-portfolio-platform` |


## Summary
Recurrence: CI job logs for the "CI" workflow on branch dev could not be retrieved (HTTP 404), identical to the incident on 2026-07-12 at commit 9bfedf1, so the actual failure at commit 06eefc2 cannot be determined.

## Root Cause
The CI logging/artifact retrieval pipeline (GitHub Actions API call or log-fetching integration used by this analysis tooling) is returning HTTP 404 when querying job logs for branch dev, meaning either the workflow run's logs have expired/been deleted, the run ID/job ID mapping used by the fetcher is stale, or the workflow itself is failing before any job/log is created (e.g., a workflow-level YAML syntax error, missing required secret, or invalid trigger condition causes GitHub to reject the run before logs are generated). This is the second occurrence in 7 days on the same branch with the same 404 symptom, indicating the prior incident was not root-caused or fixed — it was only logged as open, and no change was made to the CI configuration, log retention, or the fetch mechanism to prevent recurrence.

## Impact
No visibility into why CI is failing on dev; all merges/pushes to dev are effectively unverified since the failure reason is opaque, and any downstream deploy gating on this CI workflow is blocked without diagnostic information.

## Resolution
1. Manually inspect the GitHub Actions UI for the run at commit 06eefc2 on dev (Actions tab → CI workflow → this commit) to get the real error, since the automated fetch is broken. 2. Check `.github/workflows/ci.yml` for recent changes (diff between 9bfedf1 and 06eefc2) that could cause a malformed workflow (invalid YAML, bad `on:` trigger, missing job dependency) — a malformed workflow file often produces no job logs and shows as a 404 to API-based log fetchers. 3. Verify the GitHub token/permissions used by the internal CI-log-fetching tool has `actions:read` scope and is not hitting rate limits or an expired installation token. 4. Confirm log retention settings on the repo (Settings → Actions → General) haven't been reduced below the time window used by the fetcher. 5. Since this is a repeat 404 within a week, open a dedicated ticket to fix the log-fetching integration itself (add retry/backoff and fallback to `gh run view --log` via REST API with run ID validation) rather than re-logging the same open incident.

## Context
- **Workflow**: `CI`
- **Branch**: `dev`
- **Commit**: `06eefc2`
- **No PR** (branch push)
- **Failed step**: Unknown step
- **Triggered by**: unknown

---
*Generated by platform-agent on 2026-07-14T03:55:59.251Z*
*Labels: ci-infra, observability, recurring-incident, log-retrieval-failure, dev-branch*
