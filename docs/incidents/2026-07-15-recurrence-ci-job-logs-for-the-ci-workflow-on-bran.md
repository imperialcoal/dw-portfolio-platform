# Incident: Recurrence: CI job logs for the "CI" workflow on branch dev at commit 2733072 could not be retrieved (HTTP 404), continuing the unresolved pattern first seen 2026-07-12.

| Field | Value |
|---|---|
| **Date** | 2026-07-15 |
| **Type** | CI Failure |
| **Severity** | 🟠 HIGH |
| **Service** | `dw-portfolio-platform` |


## Summary
Recurrence: CI job logs for the "CI" workflow on branch dev at commit 2733072 could not be retrieved (HTTP 404), continuing the unresolved pattern first seen 2026-07-12.

## Root Cause
The GitHub Actions API/log retrieval is returning HTTP 404 for this workflow run, identical to the three prior incidents on 2026-07-12 (9bfedf1), 2026-07-14 (06eefc2), and 2026-07-15 (#1461103924). This is the fourth occurrence in a week with no fix applied — no diagnostic logs are available to determine the actual job failure (build, typecheck, test, or deploy step). The recurring 404 suggests either logs are expiring/being purged before retrieval, the workflow run is being deleted/cancelled before artifact upload, or the log-fetching tooling/integration itself is misconfigured (e.g., wrong run ID resolution, insufficient GITHUB_TOKEN scope, or API rate limiting) rather than an issue with the underlying CI job itself. Since no owner has addressed the root tooling issue across four incidents, this is now a systemic gap in CI observability on the dev branch.

## Impact
Root cause of the actual CI failure at commit 2733072 cannot be determined; engineers have no visibility into whether builds, typechecks, or tests are passing on dev. This blocks reliable triage of all dev-branch CI failures and has now affected at least 4 separate incidents/commits over 4 days without resolution.

## Resolution
1. Stop treating this as a per-incident CI failure and open a dedicated infra ticket to fix the log-retrieval pipeline itself. 2. Verify the GitHub Actions run for commit 2733072 directly in the GitHub UI (Actions tab) to confirm whether the run exists, was cancelled, or expired — logs expire after 90 days by default but 404s this fresh indicate a different issue (likely run deletion, permissions, or an incorrect run_id/API call in the log-fetching automation). 3. Check the GITHUB_TOKEN or PAT used by the log-fetching integration for `actions:read` scope and confirm it isn't hitting secondary rate limits. 4. Add retry-with-backoff and explicit error surfacing in the log-fetching tool so a 404 is distinguished from "job failed with no logs" vs "API call malformed." 5. Until fixed, require manual verification via GitHub UI for every dev-branch CI failure to avoid blind merges/deploys.

## Context
- **Workflow**: `CI`
- **Branch**: `dev`
- **Commit**: `2733072`
- **No PR** (branch push)
- **Failed step**: Unknown step
- **Triggered by**: unknown

---
*Generated by platform-agent on 2026-07-15T00:10:26.204Z*
*Labels: ci-infra, observability, recurring-incident, log-retrieval, dev-branch, unresolved*
