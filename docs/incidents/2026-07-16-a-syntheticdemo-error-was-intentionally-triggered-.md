# Incident: A synthetic/demo error was intentionally triggered via POST /api/demo/trigger/sentry to validate the Sentry integration pipeline; it is not an organic production failure.

| Field | Value |
|---|---|
| **Date** | 2026-07-16 |
| **Type** | Runtime Error |
| **Severity** | 🟢 LOW |
| **Service** | `dw-portfolio-preview` |
| **GitHub Issue** | https://imperial-coal.sentry.io/issues/7613953982/ |

## Summary
A synthetic/demo error was intentionally triggered via POST /api/demo/trigger/sentry to validate the Sentry integration pipeline; it is not an organic production failure.

## Root Cause
The error message "[Demo] Platform agent integration test — 1784160588279" combined with the endpoint path /api/demo/trigger/sentry indicates this is a deliberate test route (likely in app/api/demo/trigger/sentry/route.ts) that throws a hardcoded/templated Error to confirm Sentry captures errors end-to-end (SDK init, source maps, alerting). No stacktrace is available because either (1) the route throws the error without proper stack capture (e.g. Sentry.captureException(new Error(...)) called after the stack context is lost, or a plain 'throw' inside an async handler that Next.js swallows before Sentry's server instrumentation wraps it), or (2) source maps for this route were not uploaded/associated with this release, so Sentry cannot symbolicate the trace.

## Impact
No real users affected — this is a single manually/automated-triggered test event (1 occurrence) hitting an internal demo endpoint. No customer-facing feature is broken; however, if this route is publicly reachable, it represents an unintended attack surface allowing arbitrary error triggering.

## Resolution
1. Confirm intent: check app/api/demo/trigger/sentry/route.ts (or pages/api equivalent) — verify this is a test-only endpoint, e.g.:
   ```ts
   export async function POST() {
     throw new Error(`[Demo] Platform agent integration test — ${Date.now()}`);
   }
   ```
2. If intentional, restrict access: gate the route behind an env check (`if (process.env.NODE_ENV === 'production' && !isInternalRequest) return new Response('Not Found', { status: 404 })`) or require Clerk auth with an internal/admin role check via `auth()` from `@clerk/nextjs/server` before allowing the trigger.
3. Fix missing stacktrace: ensure `Sentry.captureException(err)` is called synchronously within the try/catch in the route handler rather than relying on Next.js's default error boundary, and confirm `sentry.server.config.ts` / `instrumentation.ts` initializes before the route executes.
4. Verify source map upload step in CI (e.g., `@sentry/nextjs` withSentryConfig in next.config.js) is running for this deployment/release so future real errors are symbolicated.
5. Add this route to an allowlist of "expected/test" errors in Sentry (Ignore/Resolve rule with a fingerprint on "[Demo] Platform agent integration test") to prevent alert noise going forward.

## Context
- **Error**: `Error: [Demo] Platform agent integration test — 1784160588279`
- **Culprit**: `POST /api/demo/trigger/sentry`
- **Environment**: `unknown`

- **Users affected**: 1
- **First seen**: 2026-07-16T00:09:48.282000+00:00
- **Sentry**: https://imperial-coal.sentry.io/issues/7613953982/

---
*Generated by platform-agent on 2026-07-16T00:17:03.808Z*
*Labels: sentry-integration-test, demo-endpoint, non-production-error, observability, alert-noise, security-review-needed*
