# Incident: Recurrence: CI job logs for the "CI" workflow on branch dev at commit 1732264 could not be retrieved (HTTP 404), continuing the unresolved log-retrieval pattern first seen 2026-07-12.

| Field | Value |
|---|---|
| **Date** | 2026-07-16 |
| **Type** | CI Failure |
| **Severity** | 🟡 MEDIUM |
| **Service** | `dw-portfolio-platform` |


## Summary
Recurrence: CI job logs for the "CI" workflow on branch dev at commit 1732264 could not be retrieved (HTTP 404), continuing the unresolved log-retrieval pattern first seen 2026-07-12.

## Root Cause
The CI logging/artifact retrieval pipeline for branch dev is returning HTTP 404 when querying job logs via the GitHub Actions API, identical to at least four prior incidents (2026-07-12, 2026-07-14 x2, 2026-07-15). This has persisted across multiple commits (9bfedf1, 06eefc2, 2733072, and now 1732264), indicating the underlying cause is not the CI job content itself but either: (1) workflow runs are being deleted/expired before log retrieval is attempted, (2) a GitHub Actions API permissions/token scope issue preventing the log-fetching integration from authenticating correctly, or (3) the log-fetching automation is querying the wrong run ID / a run that never completed (e.g., cancelled before job start). No prior incident report identifies which of these it is, meaning the root cause has never actually been diagnosed — only re-observed. The recurrence itself confirms no fix was ever applied to the log-retrieval tooling, only to symptoms downstream (e.g., typecheck/Dependabot job failures), which explains why it continues to reappear across unrelated triggers (manual push, Dependabot).

## Impact
The actual CI failure reason for commit 1732264 on branch dev is completely unknown — it is impossible to determine whether this is a real build/typecheck/test failure or an infrastructure issue, blocking any confident triage. This has now affected at least 5 separate CI runs over 4 days, undermining trust in CI signal on the dev branch and delaying detection of real regressions (e.g., the zod and multi-package Dependabot updates could be masking genuine breaking changes).

## Resolution


## Context
- **Workflow**: `CI`
- **Branch**: `dev`
- **Commit**: `1732264`
- **No PR** (branch push)
- **Failed step**: Unknown step
- **Triggered by**: unknown

---
*Generated by platform-agent on 2026-07-16T00:09:56.210Z*
*Labels: *
