# Incident: Demo trigger: a synthetic error was intentionally generated from the recruiter demo panel via POST /api/demo/trigger/sentry to validate the Sentry error-reporting pipeline end-to-end; this is not a real production failure.

| Field | Value |
|---|---|
| **Date** | 2026-07-19 |
| **Type** | Runtime Error |
| **Severity** | 🟢 LOW |
| **Service** | `dw-portfolio-preview` |
| **GitHub Issue** | https://imperial-coal.sentry.io/issues/7619874421/ |

## Summary
Demo trigger: a synthetic error was intentionally generated from the recruiter demo panel via POST /api/demo/trigger/sentry to validate the Sentry error-reporting pipeline end-to-end; this is not a real production failure.

## Root Cause
The endpoint /api/demo/trigger/sentry is a purpose-built demo route that manually throws or captures an Error (message: "[Demo] Platform agent integration test — 1784433441142") to confirm that errors surfaced in application code are correctly captured by Sentry and routed through the alerting/analysis pipeline. No stacktrace is available because the handler likely calls Sentry.captureException or throws a lightweight Error object without deep call context, which is expected behavior for a synthetic test event rather than an organic runtime failure.

## Impact
No real users or production features are affected. Only the single synthetic invocation (1 event) from the demo panel triggered this, used solely to validate observability tooling.

## Resolution
1. No remediation required for application logic. 2. Confirm the demo route at /api/demo/trigger/sentry (likely in app/api/demo/trigger/sentry/route.ts) is gated behind demo/recruiter auth or a non-production environment flag so it cannot be invoked in real production traffic. 3. Optionally tag these events in Sentry with a custom tag (e.g., source: "demo") at capture time so they can be filtered out of production error dashboards and alerting rules. 4. Verify alerting rules exclude this route or tag to avoid false-positive pages.

## Context
- **Error**: `Error: [Demo] Platform agent integration test — 1784433441142`
- **Culprit**: `POST /api/demo/trigger/sentry`
- **Environment**: `unknown`

- **Users affected**: 1
- **First seen**: 2026-07-19T03:57:21.145000+00:00
- **Sentry**: https://imperial-coal.sentry.io/issues/7619874421/

---
*Generated by platform-agent on 2026-07-19T04:04:56.373Z*
*Labels: demo, synthetic, sentry-test, no-action-needed*
