# Incident: A database query failed when fetching a user record by ID during a GET /platform request, likely due to a database connectivity issue or invalid/stale user ID.

| Field | Value |
|---|---|
| **Date** | 2026-08-28 |
| **Type** | Runtime Error |
| **Severity** | 🟡 MEDIUM |
| **Service** | `dw-portfolio-preview` |
| **GitHub Issue** | https://imperial-coal.sentry.io/issues/7697993640/ |

## Summary
A database query failed when fetching a user record by ID during a GET /platform request, likely due to a database connectivity issue or invalid/stale user ID.

## Root Cause
The failed query "select ... from user where user.id = $1 limit $2" indicates the Drizzle ORM query executed against Supabase Postgres could not complete successfully. Since no stacktrace is available, the most likely causes are: (1) a transient DB connection failure/timeout (Supabase pooler exhaustion or network blip between Vercel and Supabase), (2) the user ID being passed (likely from Clerk session or auth middleware resolving a user context for /platform) does not exist or references a stale/deleted record, or (3) a Postgres-level error such as connection limit exceeded or SSL/connection drop. This query pattern typically originates from a session-resolution or auth-guard helper (e.g. lib/auth/getUser.ts or a tRPC context builder in server/trpc/context.ts) called on every /platform page load to hydrate the current user. Because the error surfaced as "Failed query" rather than a Drizzle validation error, it strongly suggests the query reached the DB driver but the connection/execution itself failed rather than a schema or type mismatch.

## Impact
Currently reported for 1 user, but this query runs on every load of the /platform route (likely in a server component or middleware that resolves the authenticated user), so any transient DB/connection issue here will block access to the platform dashboard for affected users until retried. This is a core auth/session-dependent path, not a peripheral feature.

## Resolution


## Context
- **Error**: `Error: Failed query: select "id", "email", "email_verified", "name", "image", "role", "banned", "primary_org_id", "metadata", "created_at", "updated_at", "last_seen_at", "deleted_at" from "user" "user" where "user"."id" = $1 limit $2`
- **Culprit**: `GET /platform`
- **Environment**: `unknown`

- **Users affected**: 1
- **First seen**: 2026-08-28T15:47:08.654000+00:00
- **Sentry**: https://imperial-coal.sentry.io/issues/7697993640/

---
*Generated by platform-agent on 2026-08-28T15:47:55.448Z*
*Labels: *
